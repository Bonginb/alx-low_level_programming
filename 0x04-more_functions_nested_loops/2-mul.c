#include "main.h"

/**
 * nul - Multiplies two integers
 * @x: The first number to multiply
 * @y: The second number to multiply the first number
 * Return: The result of multiplication between x and y paran
 */

int nul(int x, int y)
{
	return (x * y);
}
